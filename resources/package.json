{
    "name": "plugin-staff",
    "version": "1.0.0",
    "description": "Assets du plugin Staff pour Azuriom",
    "private": true,
    "scripts": {
        "dev": "node build.mjs",
        "development": "node build.mjs",
        "watch": "node build.mjs --watch",
        "prod": "node build.mjs --prod",
        "production": "node build.mjs --prod"
    },
    "devDependencies": {
        "sass": "^1.102.0"
    },
    "dependencies": {
        "@glidejs/glide": "^3.7.1"
    }
}
